/// <reference types="vite/client" />
// Gives the test suite Vite's ambient types: `import.meta.glob` (used by the
// source-guarantee scans) and the `?raw` import suffix (used by
// setup-svelte-runtime.ts to evaluate the shipped runtime bundle).
